# This File is not working right now

# Create Ingress Route
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-router
  namespace: xmemo-system
spec:
  rules:
  - host: "test.megakuul.ch"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nginx 
            port:
              number: 80
---
apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: xmemo-system
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
---
# Create actual Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: xmemo-system
spec:
  selector:
    matchLabels:
      app: nginx
  replicas: 3
  template:
    metadata:
      namespace: xmemo-system
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        resources:
          limits:
            memory: "256Mi"
            cpu: "500m"
        image: nginx:1.14.2
        ports:
        - containerPort: 80